# Yoyo.Abp v7.4 Generation Readiness

## Input

- Official upstream repository: https://github.com/aspnetboilerplate/aspnetboilerplate/
- Official upstream tag: `v7.4`
- Local upstream input: `artifacts/yoyo-abp-migration/upstream-v7.4`

## Migration engine

- Wrapper: `tools/yoyo-abp-migration/Invoke-YoyoAbpMigration.ps1`
- Imported engine: `tools/proj-rename-ps/src2/abp-yoyo.abp-string-7.3/run.ps1`
- Output: `artifacts/yoyo-abp-migration/yoyo-v7.4`
- Focused Office regressions:
	- `tools/yoyo-abp-migration/tests/RunOffice-multilingual-map-uses-entity-key.ps1`
	- `tools/yoyo-abp-migration/Test-YoyoV74ZeroCoreSampleAppOfficeRegression.ps1`

## Checks

| Check | Result | Evidence |
| --- | --- | --- |
| Upstream input materialized | pass | `artifacts/yoyo-abp-migration/upstream-v7.4/Abp.sln` exists and the upstream snapshot still contains the full official v7.4 project tree. |
| Migration wrapper ran | pass | Fresh rerun of `tools/yoyo-abp-migration/Invoke-YoyoAbpMigration.ps1 -UpstreamPath artifacts/yoyo-abp-migration/upstream-v7.4 -OutputPath artifacts/yoyo-abp-migration/yoyo-v7.4 -CleanOutput` completed successfully and `tools/yoyo-abp-migration/logs/last-run.log` was updated. |
| Legacy solution entries removed | pass | Fresh scan over generated `artifacts/yoyo-abp-migration/yoyo-v7.4/Abp.sln` found no remaining `Abp.FluentMigrator`, `Abp.Zero.SampleApp*`, `Abp.ZeroCore.NHibernate`, or `AbpAspNetCoreDemo*` entries. |
| Root pack metadata restored | pass | Generated output root now contains `artifacts/yoyo-abp-migration/yoyo-v7.4/LICENSE.md`, satisfying the shared `PackageLicenseFile` contract from `common.props`. |
| Package IDs rewritten | pass | Generated `src/**/*.csproj` package identities remain in the `Yoyo.Abp.*` namespace and the fail-fast validator passes `PackageIdentity`. |
| Project surface matches current branch first wave | pass | Generated `src/` still contains the current 33-project first-wave surface and the fail-fast validator passes `PackageSurface`. |
| Pack surface excludes legacy packages | pass | Generated `nupkg/pack.ps1` remains aligned with the current 33-package surface and excludes `NHibernate`, `Owin`, `GraphDiff`, `FluentMigrator`, and legacy `Abp.Web*` / `Abp.Zero*` compatibility packages. |
| .NET Framework compatibility cleanup holds | pass | Fail-fast validation passes `TargetFrameworkCompatibility`; generated root/build/project files no longer carry `.NET Framework`, `net4x`, `net461`, or portable fallback remnants. |
| Office translation rewrite is self-consistent | pass | Synthetic Office rewrite regression passes, generated Office regression passes, and the generated sample app now uses `CreateMultiLingualMap<Office,string, OfficeTranslation, OfficeListDto>`, `IEntityTranslation<Office, string>`, `public string CoreId { get; set; }`, and `IRepository<OfficeTranslation, string>`. |
| Restore smoke | pass | Fresh rerun in `artifacts/yoyo-abp-migration/yoyo-v7.4` completed `dotnet restore --ignore-failed-sources` successfully. |
| Build smoke | pass | Fresh rerun in `artifacts/yoyo-abp-migration/yoyo-v7.4` completed `dotnet build Abp.sln -c Release --no-restore` successfully. |

## Decision

- Task5 build gate is now **passed**.
- Proceed to overlay verification on `verify/7.4-yoyo-on-dev-7.3.0`.
- Keep the first-wave migration boundary unchanged: current 33-package surface, `Yoyo.Abp.*` package identity, string-default primary-key semantics, no `.NET Framework` compatibility, and no legacy `NHibernate` / `Owin` / old `Abp.Zero*` / `Abp.Web*` packages.
- Treat the latest engine hardening as part of the production migration baseline: Office translation rewrite, root metadata preservation, stale solution-reference cleanup, and fail-fast validation now all participate in the successful Task5 gate.
