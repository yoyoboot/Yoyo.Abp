# Yoyo.Abp v7.4 Overlay Verification

## Verification branch

- Branch: `verify/7.4-yoyo-on-dev-7.3.0`
- Base: `codex/dev-7.3.0`
- Verification worktree: `C:\Code\yoyoboot\Yoyo.Abp\.worktrees\verify-7.4-yoyo-on-dev-7.3.0`
- Generated input: `artifacts/yoyo-abp-migration/yoyo-v7.4`

## Overlay policy

- Mirrored directories: `src/`, `test/`, `nupkg/`, `build/`, `doc/`
- Copied root files: `Abp.sln`, `common.props`, `global.json`, `Directory.Build.props`, `NuGet.Config`, `configureawait.props`, `appveyor.yml`, `azure-pipelines.yml`, `build.cmd`, `build.ps1`, `build.sh`, `README.md`, `README_CN.md`, `LICENSE.md`
- Preserved governance assets: `docs/superpowers/`, `tools/`, local workspace configuration
- Excluded generated artifacts: `nupkg/dist`, `bin`, `obj`, `dist`, `.git`, `.vs`, `node_modules`

## Checks

| Check | Result | Evidence |
| --- | --- | --- |
| Package identities match current Yoyo baseline | pass | Verification worktree `src/Abp/Abp.csproj` keeps `PackageId=Yoyo.Abp` and `AssemblyName=Yoyo.Abp`, matching the current main worktree baseline; other generated packages remain under `Yoyo.Abp.*`. |
| Project surface matches current branch first wave | pass | Overlay verification kept the current 33-project `src/` surface and the generated `nupkg/pack.ps1` remained aligned with the current first-wave package list. |
| No legacy compatibility targets or packages returned | pass | Overlay verification found no `.NET Framework` / `net4x` / `net461` / portable fallback residues and no legacy `NHibernate` / `Owin` / `GraphDiff` / `FluentMigrator` / old `Abp.Web*` / `Abp.Zero*` packages in the mirrored package surface. |
| Restore/build smoke | pass | In `verify/7.4-yoyo-on-dev-7.3.0`, both `dotnet restore --ignore-failed-sources` and `dotnet build Abp.sln -c Release --no-restore` completed successfully. |
| Governance assets preserved | pass | Verification worktree review reported no overlay changes under `docs/superpowers/` or `tools/`; both remained preserved from `codex/dev-7.3.0`. |
| Overlay diff shape | pass | `git -C C:\Code\yoyoboot\Yoyo.Abp\.worktrees\verify-7.4-yoyo-on-dev-7.3.0 status --short` returned clean after overlay verification, indicating the generated `v7.4` framework output matched the verification baseline with no additional net diff to commit. |

## Decision

- Overlay verification is **passed**.
- The generated `v7.4` output can be applied onto `codex/dev-7.3.0` without regressing package identity, first-wave package surface, governance assets, restore, or build.
- No overlay commit was created on `verify/7.4-yoyo-on-dev-7.3.0` because the verification worktree ended clean after mirroring the generated output.
- Package and downstream smoke gating has now been executed and recorded below.

## Package and downstream smoke gate

Executed package/downstream smoke summary:

| Gate | Result | Evidence |
| --- | --- | --- |
| `verify/7.4-yoyo-on-dev-7.3.0` package smoke | pass | In `C:\Code\yoyoboot\Yoyo.Abp\.worktrees\verify-7.4-yoyo-on-dev-7.3.0\nupkg`, `pwsh ./pack.ps1` completed successfully with `IS_PRODUCTION=true` and `TAG=7.4.0.1`. The run produced `33` `.nupkg` files and `33` `.snupkg` files; all parsed package versions were `7.4.0`, so the current pack script remains versioned by repository props rather than the `TAG` environment variable. |
| `C:\Code\yoyoboot\YoyoBoot` downstream smoke | pass | `dotnet restore .\Yoyo.Monorepo.sln -p:YoyoAbpVersion=7.4.0 -p:RestoreAdditionalProjectSources="C:\Code\yoyoboot\Yoyo.Abp\.worktrees\verify-7.4-yoyo-on-dev-7.3.0\nupkg"` passed, and `dotnet build .\Yoyo.Monorepo.sln -c Debug --no-restore` with the same properties also passed. |
| `C:\Code\gitea\Rider\src\aspnet-core` downstream smoke | pass with note | `YoyoBoot.Template.sln` restore/build passed with `-p:YoyoAbpVersion=7.4.0` and `RestoreAdditionalProjectSources` pointing at the local `nupkg` feed. A residual version-binding gap remains in `test/YoyoBoot.Template.Test.Base/YoyoBoot.Template.Test.Base.csproj`, which still hard-codes `Yoyo.Abp.TestBase` version `7.3.0.12`; it did not block restore/build in this smoke run, but it should be normalized before wider consumer rollout if uniform package override behavior is required. |

Representative package smoke artifacts:

- `Yoyo.Abp.7.4.0.nupkg`
- `Yoyo.Abp.AspNetCore.7.4.0.nupkg`
- `Yoyo.Abp.AspNetCore.OData.7.4.0.nupkg`
- `Yoyo.Abp.AspNetCore.PerRequestRedisCache.7.4.0.nupkg`
- `Yoyo.Abp.AspNetCore.SignalR.7.4.0.nupkg`
- `Yoyo.Abp.AspNetCore.TestBase.7.4.0.nupkg`
- `Yoyo.Abp.AutoMapper.7.4.0.nupkg`
- `Yoyo.Abp.Castle.Log4Net.7.4.0.nupkg`

Executed package smoke command from the overlay verification branch:

```powershell
$verifyRoot = '.worktrees/verify-7.4-yoyo-on-dev-7.3.0'
Push-Location "$verifyRoot/nupkg"
try {
  $env:IS_PRODUCTION = 'true'
  $env:TAG = '7.4.0.1'
  pwsh ./pack.ps1
}
finally {
  Remove-Item Env:IS_PRODUCTION -ErrorAction SilentlyContinue
  Remove-Item Env:TAG -ErrorAction SilentlyContinue
  Pop-Location
}
```

## Final gate status

- Package smoke: **passed**.
- Downstream consumer smoke: **passed** for both `YoyoBoot` and `Rider aspnet-core`.
- Residual follow-up: normalize the fixed `Yoyo.Abp.TestBase` version in `C:\Code\gitea\Rider\src\aspnet-core\test\YoyoBoot.Template.Test.Base\YoyoBoot.Template.Test.Base.csproj` if future consumers are expected to switch ABP package versions exclusively via `YoyoAbpVersion`.
- The `v7.4` first-wave migration pipeline is now closed-loop at the package/downstream smoke level; any remaining `release/7.4` decision is a publication/governance choice rather than an unresolved migration gate.